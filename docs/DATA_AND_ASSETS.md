# Data and Asset Pipeline

## Sources of Truth

| Domain | Current source |
| --- | --- |
| Item labels, IDs, aliases, loot tags, metadata | `src/data/itemDatabase.js` |
| Item runtime stats, slots, icon keys | `itemCatalog` in `src/main.js` |
| Item icon file paths | `itemTexturePaths` in `src/main.js` |
| Locations and loot lists | `locations` in `src/main.js` |
| Safehouse upgrade costs/bonuses | `upgradeData` in `src/main.js` |
| Active survivor runtime profiles | `characterProfiles` in `src/main.js` |
| Broader portrait roster | `characterDatabase` in `src/main.js` |
| Zombie visual variants | `enemyTypes` in `src/main.js` |
| House layouts | `src/data/houseMissionTemplates.js` |
| Runtime texture paths | `texturePaths` in `src/main.js` |
| Godot migration JSON | Generated under `godot_migration/data/` |

## Item Data Merge

`ITEM_DATABASE` is generated by grouping labels under loot tags, resolving canonical IDs, applying extended details, and freezing each record. At startup, records are merged into `itemCatalog`.

Important consequences:

- Database fields can override same-named runtime fields.
- A database-only item receives generic runtime defaults.
- Runtime behavior still depends on explicit fields such as `slot`, `weaponKind`, `damage`, `healHp`, `ammoType`, and texture keys.
- Metadata such as `use.effects.hunger` does nothing until a runtime system reads it.

When adding an item, update all relevant layers and test the merged result in the browser.

## Item Naming

- Canonical IDs may preserve legacy casing for compatibility.
- Player labels should be correctly spelled and consistently title-cased.
- `ITEM_ALIASES` accepts intentional historical or display variants.
- Asset filenames use snake_case.
- Avoid creating a second item because of punctuation, capitalization, or hyphen differences.

## Inventory Icons

`ITEM_ICON_STYLE_GUIDE.md` is mandatory for new and replacement icons.

Pipeline:

1. Follow the canonical order returned by `Object.keys(ITEM_DATABASE)` for the current full-list icon pass.
2. Select approved same-family icons as references.
3. Generate one centered item or one coherent item group on a flat removable chroma background.
4. Remove chroma with soft matte and despill.
5. Crop to alpha bounds, fit within the guide's safe composition box, and center on an exact `128x128` RGBA canvas.
6. Inspect transparent corners, silhouette, wear, materials, label or pictogram readability, padding, and category scale at actual inventory size.
7. Save under a new descriptive filename until approved.
8. Attach only after approval unless integration was explicitly requested. Update the exact canonical ID in `itemCatalog` and add the file path in `itemTexturePaths`.
9. Build, load the app in a browser, inspect the console, and verify the direct asset route.
10. Regenerate Godot data when migration output is in scope. An explicit request to defer Godot work takes precedence for that pass.

Do not silently overwrite approved art.

Current authoritative status (2026-07-22): the runtime-mapped icon pass extends through the Rachel and Bianca bobbleheads. Approved bobblehead mappings are Ava v2, Peter v4, Alynne v3, Luis v3, Lara v5, Jasper v5, Davis v2, Bianca v1, and Rachel v1. The detailed notes below are retained as approval history; any earlier “awaiting approval,” sequence, or filename statement is superseded by this status and `itemTexturePaths` in `src/main.js`.

The item-list pass is intentionally sequential: generate one candidate, wait for approval, attach it, then move to the next canonical item. As of 2026-07-22, icons through `Peter Bobblehead` are approved and attached (`peter_bobblehead_v1.png`); Gunpowder is a sealed dark graphite canister with a mustard label band and the corrected plain `GUNPOWDER` text, with no icon (`gunpowder_v3.png`). Weapon Parts uses a reference-informed open case containing a properly scaled long slide, shorter barrel, coiled spring, and curved trigger, with magazine/frame-like parts removed (`weapon_parts_v2.png`); Soap is one pale-blue beveled bar with embossed grooves and no bubbles (`soap_v2.png`); Plunger uses one dark red-brown rubber cup with a wooden handle and metal collar (`plunger_v1.png`); Toilet Paper is one plain off-white roll with a visible cardboard core and loose paper edge, with the prior teal label removed (`toilet_paper_v2.png`); Bag of Sugar uses a closed opaque paper bag with light-blue label accents, a sugar-cube emblem, and a clear `SUGAR` label (`bag_of_sugar_v2.png`); Bag of Salt uses a sealed transparent plastic bag with a muted-blue band, a large swan silhouette logo, and a clear `SALT` label (`bag_of_salt_v3.png`); Metal Spare Parts uses a dented gray tray of assorted washers, bolts, brackets, and a gear (`metal_spare_parts_v1.png`); Coffee Beans uses a closed, taller/slimmer bright-red-and-black coffee can with an original bean emblem (`coffee_beans_v3.png`); Fabric is one single folded blue cloth with no separate beige flap (`fabric_v4.png`). The prior `handgun_casing_v1.png` candidate remains unattached and superseded. The prior `shell_casing_v1.png` candidate remains unattached and superseded. The approved `Shell Casing` follows the reference's open star-crimped red shell body with a tarnished brass base (`shell_casing_v2.png`). The approved `Submachine Gun Casing` is a single tall, narrow spent brass casing with an empty open mouth and flared base (`submachine_gun_casing_v1.png`). The approved `Assault Rifle Casing` is one larger tapered-shoulder spent brass casing with an open mouth (`assault_rifle_casing_v1.png`). The approved `Gold Chain` is one simple continuous polished-gold necklace loop with a small clasp and no pendant (`gold_chain_v1.png`). The approved `Silver Necklace` is a long cool-silver chain with one original dragon pendant (`silver_necklace_v1.png`). The prior `diamond_ring_v1.png` and `diamond_ring_v2.png` candidates remain unattached and superseded; the approved `Diamond Ring` is a laying-down rose-gold ring with ornate shoulders and one prominent square-cut diamond, inspired by the provided reference (`diamond_ring_v3.png`). The approved `Zombie Bobblehead` uses the round-base option derived from the existing zombie sprite (`zombie_bobblehead_option_a_v1.png`); the square-plinth option (`zombie_bobblehead_option_b_v1.png`) remains unattached. The prior `ava_bobblehead_v1.png` candidate remains unattached and superseded; the approved `Ava Bobblehead` uses the v5 Ava sprite and holds a compact handgun diagonally across her chest in the main-menu-inspired ready pose (`ava_bobblehead_v2.png`). The approved `Peter Bobblehead` uses his new south-facing sprite and holds a shotgun diagonally across his torso and over one shoulder in the main-menu-inspired ready pose (`peter_bobblehead_v1.png`). The `Alynne Bobblehead` candidate uses her new south-facing sprite in a steady forward revolver-aiming stance, with the right hand extended, left hand supporting the wrist, and one eye closed along the sights (`alynne_bobblehead_v1.png`) and is awaiting approval; the next canonical items are `Luis Bobblehead` and `Dog Statue`. The Tea Box uses a worn sage-green and cream package with an original leaf emblem (`tea_box_v2.png`); the Cigarette Box uses the revised closed-pack design with a fictional crescent-and-star emblem (`cigarette_box_v3.png`); the Lighter uses the revised open, unlit flip-top design (`lighter_v3.png`); the existing approved Calculator and Walkie-Talkie icons are reused. The Ophthalmoscope revision uses the requested sleek dark-metal body and restrained blue accent reference while remaining a distinct design. The Multivitamins revision removes both red-cross symbols and uses green text while preserving the bottle, cap, sunburst, and label composition. Superseded keyboard candidates, the prior unapproved Game CD candidate, the first Syringe draft, and rejected Blood Test Kit drafts remain unattached. The pass continues through the remaining weapon-component, collectible, weapon, ammunition, backpack, and armor groups.

## Sprite Sheets

Runtime sprite sheets are horizontal PNG strips. Clip definitions provide path, frame count, and frame duration.

All new survivor reference sprites and sheets must follow [`ART_DIRECTION.md`](ART_DIRECTION.md), including the `128x128` frame standard, anatomical-side continuity, binary-alpha edges, independent per-direction review, and approval-gated assembly.

Canonical directions:

```text
north
north_east
east
south_east
south
south_west
west
north_west
```

Source GIF naming can vary, but output filenames must use these direction keys.

Relevant tools:

- `tools/build_sprite_sheets.py`
- `tools/build_male_sprite_sheets.py`
- `tools/build_alynne_sprite_sheets.py`
- `tools/build_luis_rotation_sheets.py`
- `tools/build_ava_pickup_sheets.py`
- `tools/build_zombie_sprite_sheets.py`

Validation:

- Correct frame count and order.
- Consistent frame dimensions.
- Chroma background removed cleanly.
- Alpha retained.
- No interpolation blur.
- Direction matches the filename.
- Final action duration matches movement lock.

## Portraits

Active runtime portraits are imported through Vite at the top of `src/main.js`. Future portraits remain in `assets/portraits/` and `characterDatabase` until a sprite/animation profile makes them playable.

Portrait-processing tools:

- `tools/build_character_portraits.py`
- `tools/prepare_character_portrait.py`

Keep portrait framing consistent across inventory and Rest Station layouts.

## Environment Textures

Environment textures live in `assets/textures/`. Reuse existing maps before creating duplicates.

Patterns:

- Single-map materials use `createTextureMaterial()`.
- PBR-style props can use matching albedo, bump, and roughness files.
- Color/albedo maps use sRGB color space.
- Data maps such as bump and roughness remain linear.
- Repeating textures use `RepeatWrapping` and stable physical scale.

Relevant tools:

- `tools/generate_game_textures.py`
- `tools/generate_rest_station_assets.py`
- `tools/build_base_door_maps.py`
- `tools/build_base_fence_maps.py`
- `tools/build_crate_maps.py`

Preserve overwrite guards. Generated fallback art must not replace approved custom textures accidentally.

## Native Props and External Meshes

The current wooden supply crate is generated with lightweight Three.js geometry and shared materials. It uses:

- `crate_wood_albedo.png`
- `crate_wood_bump.png`
- `crate_wood_roughness.png`
- Existing workbench steel texture for hardware

Previously supplied Meshy GLBs were rejected because they were extremely dense and lacked game-ready UV/material data. Do not import external meshes without checking:

- Triangle and vertex count.
- UV coordinates.
- Material and texture presence.
- Static/skeleton/animation structure.
- Dimensions and axis orientation.
- Bottom-centered pivot.
- Visual quality at the actual isometric camera distance.

For small static props, native Three.js geometry can be more maintainable than an opaque generated mesh.

## Audio

Audio files live in `assets/audio/` and are imported through Vite when referenced as modules. Background tracks loop by game mode; effects are cloned from preloaded source elements.

Use short, normalized effects. Verify browser autoplay behavior through real pointer/keyboard interaction.

## House Templates

Reference images live in `assets/map_templates/house/`. Runtime definitions live in `src/data/houseMissionTemplates.js`.

A template includes:

- Unique ID and display name.
- Entrance room/side.
- Grid-positioned room records.
- Explicit connections.
- Furniture placements.
- Container and loose-loot sockets with probability.

When adding a template, verify graph connectivity, door openings, exterior entrance, furniture clearance, loot clearance, and extraction placement.

## UI Assets and Style

UI-specific art lives in `assets/ui/`. Most station styling is CSS-driven.

The learned visual language is:

- Thick charcoal/gunmetal frame.
- Layered bevels and recessed dark panels.
- Small rivets and fasteners.
- Restrained edge rust.
- Compact squared silhouettes.
- Warm off-white text and muted survival-status colors.

The downloaded reference atlas/individual PNG pack is not a runtime dependency unless direct use is explicitly requested.

## Godot Export

Run after relevant data or asset changes:

```bash
node tools/export_godot_data.mjs
```

Generated outputs include items, item textures, locations, characters, enemy types, animations, textures, upgrades, constants, and an asset manifest.

Review the JSON diff. A large asset-manifest rewrite can be legitimate after adding icons, but it should be understood before committing.

## Asset Checklist

- Correct directory and filename.
- Correct dimensions and color mode.
- Alpha where expected.
- No unintended border, background, or watermark.
- Runtime mapping updated.
- Existing art not overwritten without permission.
- Browser console has no missing-texture warning.
- Production build succeeds.
- Godot data regenerated where relevant.

The original `alynne_bobblehead_v1.png` candidate did not match Alynne and remains unattached and superseded. The revised `Alynne Bobblehead` uses the attached south-facing sprite identity (long black hair, blue-black pinstriped wrap blouse with owl-heart emblem, black pants, and boots) in a steady two-handed revolver stance, with her torso and aim shifted slightly toward the viewer's right and one eye closed along the sights (`alynne_bobblehead_v2.png`); it is awaiting approval.
The revised `Alynne Bobblehead` is approved and attached as `alynne_bobblehead_v2.png`.
This approval supersedes the earlier awaiting-approval wording in the sequential item-list paragraph.
The `Luis Bobblehead` candidate uses the attached Luis sprite identity in a two-handed M4A1 aiming stance, with his torso and aim shifted slightly toward the viewer's left, head aligned to the sights, and one eye closed (`luis_bobblehead_v1.png`); it is awaiting approval. The next canonical items are `Dog Statue`, `Tiger Statue`, and `Teddy Bear`.
The `luis_bobblehead_v1.png` candidate is superseded because the rifle overlapped his shoulder. Revised candidate `luis_bobblehead_v2.png` keeps the full M4A1 visible in front of his body and shifts his head, torso, and legs coherently toward the viewer's right; it is awaiting approval.
The revised `Luis Bobblehead` is approved and attached as `luis_bobblehead_v2.png`.
This approval supersedes the earlier awaiting-approval wording for Luis in the sequential item-list paragraph.
The `Lara Bobblehead` candidate uses Lara's south-facing sprite identity and the main-menu-inspired raised-flashlight pose (`lara_bobblehead_v1.png`); it is awaiting approval. The next canonical items remain `Dog Statue`, `Tiger Statue`, and `Teddy Bear`.
The `lara_bobblehead_v1.png` candidate is superseded because Lara was missing her handgun. Revised candidate `lara_bobblehead_v2.png` adds a compact handgun clearly held in her right hand (viewer’s left) at hip height while preserving the raised flashlight pose; it is awaiting approval.
The revised `Lara Bobblehead` is approved and attached as `lara_bobblehead_v2.png`. The collectible sequence is revised so the new character bobbleheads follow Luis; the next canonical bobblehead is `Jasper Bobblehead`, followed by `Davis Bobblehead`, `Rachel Bobblehead`, and `Bianca Bobblehead` before the statue collectibles. This supersedes earlier sequence wording that placed the statues immediately after Luis or Lara.
The `Jasper Bobblehead` candidate uses Jasper's approved south-facing sprite and the main-menu-inspired lower-left axe pose (`jasper_bobblehead_v1.png`); it is awaiting approval. The next canonical items are `Davis Bobblehead` and `Rachel Bobblehead`.
The `jasper_bobblehead_v1.png` candidate is superseded. Revised candidate `jasper_bobblehead_v2.png` preserves the axe pose but changes Jasper to a visibly scared expression and defensive startled posture; it is awaiting approval.
The `jasper_bobblehead_v2.png` candidate is superseded. Revised candidate `jasper_bobblehead_v4.png` keeps the same scared defensive axe pose while applying Jasper's absolute likeness lock from the supplied reference: center-parted medium chestnut-brown curtain hair, green eyes, clean-shaven refined face, and approved wardrobe; it is awaiting approval.
The intermediate `jasper_bobblehead_v3.png` candidate is also superseded by v4.
The revised `Jasper Bobblehead` is approved and attached as `jasper_bobblehead_v4.png`.
The current `peter_bobblehead_v1.png` is being revisited against the newly supplied Peter reference; a replacement candidate will preserve the shotgun pose while matching Peter's brown side-swept hair, blue-gray suit, white shirt, red tie, blue eyes, and brown shoes.
The `peter_bobblehead_v1.png` candidate is superseded. Revised candidate `peter_bobblehead_v2.png` preserves the diagonal shotgun pose while matching the supplied Peter reference's brown side-swept hair, blue-gray suit, white shirt, red tie, blue eyes, and brown shoes; it is awaiting approval.
The bobblehead style is now locked to the approved `Ava Bobblehead` (`ava_bobblehead_v2.png`): compact chibi proportions, oversized head, small body, rounded dark base, crisp pixel-art outline, controlled saturation, cel-shaded highlights, and consistent inventory padding. This style lock applies to Peter, Alynne, Luis, Lara, and Jasper while preserving each character's own identity, clothing, equipment, and pose. Revised Peter candidate `peter_bobblehead_v3.png` applies this style lock and is awaiting approval.
The `peter_bobblehead_v3.png` candidate is superseded. Revised candidate `peter_bobblehead_v4.png` changes only Peter's face to a more mature, angular portrait-matched expression with stronger brow, jaw, eye definition, and subtle facial lines; the Ava-style body, clothing, shotgun, pose, base, and shading remain unchanged. It is awaiting approval.
The revised `Peter Bobblehead` is approved and attached as `peter_bobblehead_v4.png`.
The revised `Alynne Bobblehead` candidate `alynne_bobblehead_v3.png` applies the Ava style lock while preserving Alynne's long black hair, blue-black owl-heart wrap blouse, black pants and boots, two-handed revolver aim toward the viewer's right, and sight-aligned closed eye; it is awaiting approval.
The revised `Alynne Bobblehead` is approved and attached as `alynne_bobblehead_v3.png`.
The revised `Luis Bobblehead` candidate `luis_bobblehead_v3.png` applies the Ava style lock while preserving Luis's short black hair, mustache and goatee, brown jacket, black shirt, dark blue jeans, two-handed M4A1 stance, and viewer-left aim; it is awaiting approval.
The revised `Luis Bobblehead` is approved and attached as `luis_bobblehead_v3.png`.
The revised `Lara Bobblehead` candidate `lara_bobblehead_v4.png` applies the Ava style lock while preserving Lara's long dark hair, black outfit, raised flashlight, hip-level handgun, and serious gaze toward the flashlight direction; it is awaiting approval.
The supplied Lara bobblehead image was the locked source for the final revision. Approved `lara_bobblehead_v5.png` changes only Lara's eye direction toward the flashlight and her expression to serious/focused; her pose, clothing, equipment, proportions, base, and framing remain unchanged. It is attached in the runtime texture mapping.
The `Davis Bobblehead` candidate `davis_bobblehead_v1.png` applies the locked Ava bobblehead style to Davis's approved south-facing firefighter identity, preserving his mature face, gray hair and beard, yellow station uniform, badge, patch, red bands, cargo trousers, belt, and work boots in a sturdy neutral standing pose. It is awaiting approval and remains unattached.
The `davis_bobblehead_v1.png` candidate is superseded. Approved `davis_bobblehead_v2.png` adds exactly one dark steel crowbar held across Davis's front while preserving all other character, style, pose, base, and framing details. It is attached in the runtime texture mapping.
The previously approved `jasper_bobblehead_v4.png` was revisited under the locked Ava-style rules. Approved `jasper_bobblehead_v5.png` preserves Jasper's absolute identity, restrained scared expression, lower-left axe pose, clothing, equipment, and base while refreshing the rendering style. It is attached in the runtime texture mapping.
The approved `Bianca Bobblehead` uses `bianca_bobblehead_v1.png`, applying the locked Ava style to Bianca's approved identity and dress, with one compact Uzi held in her anatomical-right hand (viewer-left) and pointed upward. Bianca has a calm expression with a small confident smirk. It is attached in the runtime texture mapping.
The approved `Rachel Bobblehead` uses `rachel_bobblehead_v1.png`, applying the locked Ava style to Rachel's approved identity, with one hand supporting an open white metallic first-aid kit and the other hand visibly rummaging inside. The red-cross kit follows `first_aid_kit_v2.png`; Rachel has a calm, focused expression. It is attached in the runtime texture mapping.
